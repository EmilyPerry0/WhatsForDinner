<!doctype html>
<html>
  <head>
    <title>{{ title }} — WhatsForDinnerC+E</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="{{ '/styles.css' | url }}" />
  </head>

  <body>
    <main class="recipe">
      <p><a href="{{ '/' | url }}" class="back_link">&larr; Back to the wheel</a></p>
      <h1>{{ title }}</h1>
      {{ content | safe }}
    </main>
  </body>
</html>
